services:
  - type: static
    name: maria-skills-website
    runtime: node
    buildCommand: npm install && npm run build
    staticPublishPath: out
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
